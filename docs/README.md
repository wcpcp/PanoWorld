# PanoWorld Project Page

This `docs/` directory is a GitHub Pages-ready project page draft for:

- `PanoWorld: Towards Spatial Supersensing in 360° Panorama World`

## Included assets

- `assets/teaser.png`
- `assets/architecture.png`
- `assets/dataset.png`
- `assets/panoworld_paper.pdf`

## Before publishing

Please update the following in [`index.html`](/Users/wcp/code/erp_data_pipeline/docs/index.html):

1. Dataset / benchmark release links
2. Model / checkpoint links
3. Any final venue wording if it changes from arXiv-only release

## GitHub Pages

For the `wcpcp/PanoWorld` repository, the simplest setup is:

1. Copy this whole `docs/` directory into the repo root
2. Push to the default branch
3. In GitHub repository settings, enable Pages from the `/docs` folder

Then the page should be served automatically.
